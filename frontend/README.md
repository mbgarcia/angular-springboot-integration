# Frontend Angular

Starter Angular project deve ser criado pelo candidato.

Steps:
1. Config local angular version
   npm install @angular/cli@19
2. Check the version
   ng version
3. Create a project
   ng new 
