import { Routes } from '@angular/router';
import { BenefitsComponent } from './benefits/benefits.component';

export const routes: Routes = [
    {path: 'benefits', component: BenefitsComponent, title: 'Benefits Page'}
];
